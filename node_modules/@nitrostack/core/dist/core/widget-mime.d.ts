/**
 * MIME type for interactive MCP / ChatGPT UI resources (SEP-1865).
 * Hosts that load "MCP App" iframes reject plain `text/html`.
 */
export declare const MCP_APP_WIDGET_MIME_TYPE = "text/html;profile=mcp-app";
/**
 * Legacy OpenAI Apps SDK / Skybridge HTML resources (when not using MCP App profile).
 */
export declare const OPENAI_SKYBRIDGE_WIDGET_MIME_TYPE = "text/html+skybridge";
/** Plain HTML — what many OpenAI / ChatGPT widget hosts expect in resources/list. */
export declare const OPENAI_PLAIN_HTML_WIDGET_MIME_TYPE = "text/html";
/** @alias Default NitroStack widget resource MIME (MCP App profile) */
export declare const WIDGET_MIME_TYPE = "text/html;profile=mcp-app";
/** @alias Same as {@link MCP_APP_WIDGET_MIME_TYPE} */
export declare const MCP_APPS_MIME_TYPE = "text/html;profile=mcp-app";
/** @alias Skybridge-only */
export declare const OPENAI_SKYBRIDGE_MIME_TYPE = "text/html+skybridge";
/**
 * Widget HTML MIME for `resources/read` + tool metadata.
 *
 * - Default / `mcp-app` / unset: `text/html;profile=mcp-app` (MCP Apps, MCPJam Inspector)
 * - `openai`: `text/html+skybridge` (ChatGPT / OpenAI Apps SDK pattern; see repo `todo-openai-apps-sdk-example`)
 * - `legacy` / `plain` / `text/html`: plain `text/html` (hosts that reject skybridge)
 * - `skybridge`: same as `openai`
 *
 * Override per component with {@link ComponentMetadata.widgetResourceMime}.
 */
export declare function resolveWidgetResourceMimeType(explicit?: string): string;
//# sourceMappingURL=widget-mime.d.ts.map