import http from 'http';
interface Logger {
    info(message: string, ...args: unknown[]): void;
    warn(message: string, ...args: unknown[]): void;
    error(message: string, ...args: unknown[]): void;
}
export interface DiscoveryServerOptions {
    /** Preferred port to start on */
    port?: number;
    /** If true, try next available port if preferred port is in use */
    autoRetry?: boolean;
    /** Maximum number of ports to try */
    maxRetries?: number;
}
export declare class DiscoveryHttpServer {
    private logger;
    private server;
    private handlers;
    private _actualPort;
    private options;
    constructor(portOrOptions: number | DiscoveryServerOptions, logger: Logger);
    private handleRequest;
    on(path: string, handler: http.RequestListener): void;
    /**
     * Get the actual port the server is running on
     * (may differ from configured port if auto-retry found an available port)
     */
    getPort(): number;
    start(): Promise<void>;
    stop(): Promise<void>;
}
export {};
//# sourceMappingURL=discovery-http-server.d.ts.map