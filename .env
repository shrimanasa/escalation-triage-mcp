# Path to your SQLite file. Currently pointing at the pre-seeded demo DB
# (data/escalation.db) which already has 3 demo customers, owners, and bugs
# loaded and matches data/expected_schema.sql exactly — no edits to src/db.ts
# were needed.
DB_PATH=./data/escalation.db
PORT=3001